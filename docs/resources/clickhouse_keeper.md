---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "altinity_clickhouse_keeper Resource - Altinity.Cloud"
subcategory: ""
description: |-
  A ClickHouse Keeper (CH Keeper) coordination cluster. Reference it from an altinity_clickhouse_cluster via keeper_name.
---

# altinity_clickhouse_keeper (Resource)

A ClickHouse Keeper (CH Keeper) coordination cluster. Reference it from an altinity_clickhouse_cluster via keeper_name.

## Example Usage

```terraform
# A CH Keeper coordination cluster. Reference it from a cluster via keeper_name.
#
# `ha` is intentionally absent — it is Computed-only (ACM auto-promotes based on
# the bound cluster's replica count). Adding `ha = true` will fail at plan time.
resource "altinity_clickhouse_keeper" "example" {
  environment   = data.altinity_environment.this.id
  name          = "shared-keeper"
  instance_type = "e2-standard-2" # zookeeper-scoped node type (see altinity_node_types)

  # `zones` is optional and platform-specific. Omit to let ACM pick — or list
  # the environment's az list (see the altinity_zones data source) to spread
  # replicas explicitly. The literal values below are GCP zones; on AWS/Azure
  # use the corresponding zone codes.
  # zones = data.altinity_zones.this.zones
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `environment` (String) ACM environment id the keeper lives in.
- `name` (String) Keeper name (unique within the environment). Used as a URL path segment, so must not contain `/`, `:`, or whitespace.

### Optional

- `image` (String) Keeper image/version.
- `instance_type` (String) Node instance type — a zookeeper-scoped type (e.g. e2-standard-2); see the altinity_node_types data source.
- `timeouts` (Attributes) Operation timeouts (Go duration strings). (see [below for nested schema](#nestedatt--timeouts))
- `zones` (List of String) Availability zones to spread keeper nodes across.

### Read-Only

- `cpu_limits` (String) Read-back CPU limit (k8s quantity).
- `cpu_requests` (String) Read-back CPU request (k8s quantity).
- `ha` (Boolean) High availability (3-node) keeper when true; single-node otherwise. ACM-managed: the value is determined server-side from the bound cluster's replica count (any cluster with replicas > 1 forces HA) and reported back here. Cannot be set in config.
- `id` (String) Resource id, "<environment>:<name>".
- `zone_topology_key` (String) Read-back zone topology key.

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String)
- `delete` (String)
- `update` (String)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Keepers are imported by "<environment>:<name>".
terraform import altinity_clickhouse_keeper.example 2267:shared-keeper
```
