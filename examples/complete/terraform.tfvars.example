# Copy to terraform.tfvars and fill in. Do NOT commit real secrets.
# Admin/user passwords are generated by random_password (see main.tf), so only
# the API token + environment name are required.
altinity_api_token = "acm-xxxxxxxxxxxxxxxx"
environment_name   = "your-environment-name"
